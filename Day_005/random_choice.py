# choose a random element from a list using choice method

import random

name = ["amit", "akash", "prateek", "aman"]
print(random.choice(name))

# benefit is only that we do not have to create a random number and then look for an element from the list using that random index generated.
